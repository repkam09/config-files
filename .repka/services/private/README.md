# Repka Private Services

Caddy Webserver setup for services available on the *.repkam09.dev domain, only avaiable inside the VPN. All reverse_proxy setup should point to a VPN IP Address. 

The *.repkam09.dev resolves to VPN IP Address of `repka-node`.

