#!/bin/bash
BASE=`echo "$(lsb_release -is)"`

if [ $BASE == "Ubuntu" ]
then
systemctl poweroff
fi


if [ $BASE == "Manjaro-ARM" ]
then
systemctl poweroff
fi

if [ $BASE == "Manjaro" ]
then
systemctl poweroff
fi
